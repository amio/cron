{
  "name": "cron",
  "description": "Amio's cron jobs",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "amio/cron",
  "dependencies": {
    "axios": "^1.3.5",
    "words-en": "^2.0.0"
  }
}
